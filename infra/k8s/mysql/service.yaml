apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: finsight-k8s
  labels:
    app: mysql
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
