apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: finsight-k8s
  labels:
    app: web
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
