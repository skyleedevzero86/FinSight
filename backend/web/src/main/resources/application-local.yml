spring:
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:9379}
      password: ${REDIS_PASSWORD:123456}
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

logging:
  level:
    com.sleekydz86.finsight: DEBUG
    org.springframework.batch: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

jwt:
  secret: jwtscretjwtscretjwtscretjwtscretjwtscretjwtscretjwtscretjwtscretjwtscretjwtscretjwtscretjwts
  expiration-period: 3600000
  refresh-expiration-period: 86400000

jasypt:
  encryptor:
    password: "your-jasypt-encryption-password-here"

ai:
  djl:
    model:
      name: cardiffnlp/twitter-roberta-base-sentiment-latest
    engine: pytorch
    device: cpu
    performance:
      enable-caching: true
      cache-size: 500
  huggingface:
    models:
      sentiment:
        name: cardiffnlp/twitter-roberta-base-sentiment-latest
    performance:
      enable-caching: true
      cache-size: 500